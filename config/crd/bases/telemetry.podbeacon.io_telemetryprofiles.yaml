---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  name: telemetryprofiles.telemetry.podbeacon.io
spec:
  group: telemetry.podbeacon.io
  names:
    kind: TelemetryProfile
    listKind: TelemetryProfileList
    plural: telemetryprofiles
    singular: telemetryprofile
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Is the profile ready
      jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - description: Generated ConfigMap name
      jsonPath: .status.configMapName
      name: ConfigMap
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: TelemetryProfile is the Schema for the telemetryprofiles API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TelemetryProfileSpec defines the desired state of TelemetryProfile
            properties:
              batch:
                description: Batch configures the batch processor
                properties:
                  sendBatchSize:
                    default: 512
                    description: SendBatchSize is the number of spans/metrics/logs
                      to batch. Default 512.
                    format: int32
                    maximum: 8192
                    minimum: 1
                    type: integer
                  timeout:
                    default: 5s
                    description: Timeout is the time to wait before flushing a batch.
                      Default 5s.
                    format: duration
                    type: string
                type: object
              exporter:
                description: Exporter configures the OTLP destination
                properties:
                  endpoint:
                    description: Endpoint is the required OTLP/gRPC host:port
                    minLength: 1
                    pattern: ^[a-zA-Z0-9.-]+:\d+$
                    type: string
                  headersSecretRef:
                    description: HeadersSecretRef points to a Secret whose keys map
                      to outbound header names.
                    properties:
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                  queueSize:
                    default: 256
                    description: QueueSize is the number of batches to queue. Default
                      256, max 4096.
                    format: int32
                    maximum: 4096
                    minimum: 1
                    type: integer
                  retryMaxElapsedTime:
                    default: 30s
                    description: RetryMaxElapsedTime is the maximum time spent retrying
                      a batch. Default 30s.
                    format: duration
                    type: string
                  tls:
                    description: TLS configuration for the exporter
                    properties:
                      caSecretRef:
                        description: CASecretRef points to a Secret in the same namespace
                          containing the CA cert
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                      insecure:
                        description: Insecure disables TLS verification when set to
                          true. Default is false.
                        type: boolean
                    type: object
                required:
                - endpoint
                type: object
              resources:
                default:
                  limits:
                    cpu: 100m
                    memory: 128Mi
                  requests:
                    cpu: 10m
                    memory: 64Mi
                description: Resources configures the collector sidecar resources
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This field depends on the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              signals:
                default:
                - traces
                - metrics
                description: Signals is a unique subset of traces, metrics, logs
                items:
                  description: SignalType defines the telemetry signal
                  enum:
                  - traces
                  - metrics
                  - logs
                  type: string
                maxItems: 3
                minItems: 1
                type: array
                x-kubernetes-list-type: set
            required:
            - exporter
            type: object
          status:
            description: TelemetryProfileStatus defines the observed state of TelemetryProfile
            properties:
              conditions:
                description: Conditions represent the current state of the TelemetryProfile
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              configHash:
                description: ConfigHash is the hash of the inputs used to generate
                  the current ConfigMap.
                type: string
              configMapName:
                description: ConfigMapName is the name of the generated ConfigMap
                  containing the collector config.
                type: string
              observedGeneration:
                description: ObservedGeneration is the latest generation observed
                  by the controller.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
